import React from 'react';
import styled from 'styled-components';

export const ErrorPage = () => {
  return <BaseContainer>Lol no</BaseContainer>;
};

const BaseContainer = styled.div`
  vertical-align: top;
  position: relative;
  margin: 0 auto;
  width: 100%;
  height: 100%;
`;
